你是一名资深前端UI/UX工程师，精通 Vue3 + Element Plus + 管理后台系统设计。

目标：
在不改变业务逻辑、不修改接口调用、不修改字段含义的前提下，
优化并重构当前页面的显示效果，使其更像专业后台管理系统，
提升视觉层级、排版规范和用户体验。

【重要限制】
1. 绝对不能修改接口地址、请求参数、返回处理逻辑
2. 不改变现有变量名
3. 不删除已有功能
4. 只优化结构、样式、组件组合和交互体验

----------------------------------

【页面结构优化要求】

统一后台页面结构：

1. 页面顶部：
   - 页面标题（如：用户管理）
   - 简短说明文字
   - 主操作按钮（新增/导出）

2. 查询筛选区：
   - 使用 el-card 包裹
   - 表单布局对齐整齐
   - 支持重置按钮
   - 可适当支持折叠高级查询

3. 表格区：
   - 使用 el-card 包裹
   - 表格行高 48~56
   - 列对齐清晰
   - 状态字段使用 el-tag 显示
   - 操作列主次分明
   - 危险操作使用 el-popconfirm

4. 分页：
   - 统一放在表格底部右侧
   - 样式简洁

----------------------------------

【视觉规范】

1. 间距使用 8 / 12 / 16 / 24 的倍数
2. 卡片圆角 12~16
3. 统一按钮尺寸
4. 主色使用 Element Plus 默认主题
5. 避免花哨颜色
6. 标题、副标题、正文层级清晰
7. 使用轻量阴影增强层级

----------------------------------

【交互优化】

1. 添加 loading 状态（v-loading）
2. 添加空数据提示（el-empty）
3. 表单弹窗关闭时自动重置
4. 表格操作按钮分组（主操作 + 更多操作）
5. 危险操作必须二次确认

----------------------------------

【可选增强】

1. 
2. 添加简单动画过渡
3. 增强响应式布局

----------------------------------

【输出要求】

1. 输出完整可运行的 Vue 单文件组件（SFC）
   - <template>
   - <script setup>
   - <style scoped>
2. 不使用额外UI框架
3. 保持当前数据结构
4. 最后用要点说明做了哪些优化（不超过10条）

----------------------------------


